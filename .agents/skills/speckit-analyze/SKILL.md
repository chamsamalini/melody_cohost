---
name: speckit-analyze
description: Use when checking consistency across Spec Kit spec.md, plan.md, and tasks.md before implementation.
mode: speckit.analyze
---

# Spec Kit Analyze

Read and follow `.specify/templates/commands/analyze.md`.

When the command references `scripts/powershell/check-prerequisites.ps1`, run `.specify/scripts/powershell/check-prerequisites.ps1` from the repository root.

This skill is read-only: report issues without modifying files unless the user asks for fixes.
