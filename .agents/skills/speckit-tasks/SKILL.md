---
name: speckit-tasks
description: Use when generating an actionable Spec Kit tasks.md from a feature plan and design artifacts.
mode: speckit.tasks
---

# Spec Kit Tasks

Read and follow `.specify/templates/commands/tasks.md`.

When the command references `scripts/powershell/setup-tasks.ps1`, run `.specify/scripts/powershell/setup-tasks.ps1` from the repository root.

Tasks must use the official checklist format and name exact files in this project.
